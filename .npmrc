; mem0ai (optional) pins an older @anthropic-ai/sdk peer than we use directly.
; mem0ai is loaded via dynamic import and degrades gracefully, so we relax peer
; resolution rather than downgrade the Anthropic SDK the agent depends on.
legacy-peer-deps=true
